{% extends "layout.njk" %}

{% block head %}

        <title>Conteúdo - Desafio 3</title>
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
        rel="stylesheet">

{% endblock %}

{% block content %}
            
        <div class="card">
            {% for item in items %}
            <div class="logo" id="{{item.id}}">
                <img src="{{item.img_src}}" alt="{{item.title}}">
                <p>{{item.price}}</p>
            </div>
            {% endfor %}
        </div>
        <script src="/scripts.js"></script>

{% endblock %}